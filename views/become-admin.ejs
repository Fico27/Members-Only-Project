<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Become an Admin</title>
  </head>
  <body>
    <header>
      <div>
        <h1><i>The</i> Anon Club</h1>
        <ul>
          <li><a href="/messageboard">Message Board</a></li>
        </ul>
      </div>
    </header>
    <main>
      <form action="/newadmin" method="post">
        <h2>Become an Admin here!</h2>
        <p>You just need to know the <i>password</i>!</p>
        <% if (message) { %>
        <p style="color: red"><%= message%></p>
        <% } %>

        <label for="password">Enter the Secret Admin Password</label>
        <input type="password" name="password" id="assword" />

        <button type="submit">Submit</button>
      </form>
    </main>
  </body>
</html>
